# Python virtualenv (kept locally, never committed — recreate with requirements.txt)
.venv/
__pycache__/
*.pyc

# Go build artifacts
*.exe
/vnstock-service
/backfill
/update

# Local env / secrets
.env

# Claude Code local (machine-specific) settings — shared settings go in settings.json
.claude/settings.local.json

# Scratch / helper output
*.log
