# Python deps for the vnstock bridge (python/fetch.py).
# Use Python 3.11 (NOT 3.14 — its only Windows numpy build segfaults).
#
#   py -3.11 -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt
#
# vnstock pulls in pandas/numpy/requests etc. transitively; pinning vnstock
# alone is enough to reproduce the environment.
vnstock==4.0.4
